name: 'listify-actions'
description: 'Generate and merge all directories to the specified file.'
icon: align-left
color: blue
inputs:
  listify:
    description: 'listify config file'
    required: true
    default: './listify.yml'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.listify }}
